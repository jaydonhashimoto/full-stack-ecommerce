import React, { Component } from 'react';
import EBookList from '../EBookList';

export class Home extends Component {
  render() {
    return (
      <div>
        <EBookList />
      </div>
    );
  }
}

export default Home;
